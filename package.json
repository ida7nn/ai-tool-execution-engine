{
  "name": "ai-tool-execution-engine",
  "version": "1.0.0",
  "private": false,
  "description": "Production-oriented TypeScript example of a multi-tenant AI tool execution layer.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^7.0.2",
    "vitest": "^2.1.8"
  }
}
